# dependencies (produced by INSTALL.cmd / npm / pnpm on the target machine)
node_modules/

# local renderer scratch + outputs
.dsh-md2pdf-tmp/
*.pdf

# editor / OS noise
.vscode/
.idea/
Thumbs.db
.DS_Store

# local build artifacts (not part of the distributable)
.build/

# python bytecode / local scratch (regenerated at runtime)
__pycache__/
*.pyc

# Python / local build artifacts (regenerated by running the scripts or by a
# compile sanity-check) — never part of the distributable preset.
__pycache__/
*.pyc

# Consolidation output directory (the agent writes consolidated.json/.csv/.xlsx
# here at runtime) — user data, never shipped.
work/

# OS noise
.DS_Store
